import { catchMessage } from 'fyb/helpers/catch-message';
import { module, test } from 'qunit';

module('Unit | Helper | catch message');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = catchMessage([42]);
  assert.ok(result);
});
